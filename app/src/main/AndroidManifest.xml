<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cegep.lanbow">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.admin.SearchUsers"></activity>
        <activity android:name=".activities.admin.AddItem" />
        <activity android:name=".activities.student.UserProfile" />
        <activity android:name=".activities.student.EmailVerify" />
        <activity android:name=".activities.admin.AdminHome" />
        <activity android:name=".activities.student.UserHome" />
        <activity android:name=".activities.admin.AdminLogin" />
        <activity android:name=".activities.student.StudentRegister" />
        <activity android:name=".activities.student.StudentLogin" />
        <activity android:name=".activities.GettingStarted">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>